(* Returns the factorial of n. *)
let rec fact (n : int) : int =
  failwith "unimplemented"

(* Returns an unary representation of n.
   Assume that n is non-negative. *)
let rec unary (n : int) : string =
  failwith "unimplemented"

(* Returns the sum of the digits of n. *)
let rec sum_digits (n : int) : int =
  failwith "unimplemented"

(* Returns a binary representation of n.
   Assume that n is non-negative. *)

let rec binary (n : int) : string =
  failwith "unimplemented"
